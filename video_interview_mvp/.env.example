# AI services are optional: the application has mock fallbacks for local demos.
LLM_API_KEY=
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=qwen/qwen-2.5-72b-instruct
DEEPGRAM_API_KEY=

# No shared HR/manager registration keys are required.
# The first account becomes the workspace HR owner; afterwards HR generates
# one-time invitation links from the workspace.
